(ns cmr.metadata-db.int-test.concepts.association-spec
  "Defines a common set of tests for working with association concepts in metadata-db."
  (:require
   [clojure.test :refer :all]
   [cmr.common.concepts :as cc]))
